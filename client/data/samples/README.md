# 示例导入数据

此目录存放可直接导入 Narrative Mirror 的 JSON 文件。

## realtalk_emi_elise.json

- **来源**：narrarc 的 REALTALK 评估数据集
- **内容**：Elise 与 Emi 的 476 条英文对话
- **用途**：在客户端「导入数据」中选择此文件，可快速体验叙事查询功能

导入后需等待后台构建（L1 → L1.5 → L2）完成，build_status 变为 `complete` 后即可发起查询。
